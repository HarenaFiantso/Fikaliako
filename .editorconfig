root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

# Kotlin — ktlint official code style (enforced by Spotless in apps/api)
[*.{kt,kts}]
max_line_length = 100

[*.md]
trim_trailing_whitespace = false
